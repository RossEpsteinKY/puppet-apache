{
  "puppet_task_version": 1,
  "supports_noop": false,
  "description": "Allows user to run apachectl in puppet",
  "parameters": {
    "command": {
      "description": "Which apachetctl command to run",
      "type": "Enum[start,stop,restart,status,graceful,graceful-stop]"
    }
  }
}
